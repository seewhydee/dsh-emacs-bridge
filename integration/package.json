{
  "name": "dsh-emacs-bridge-integration",
  "version": "0.0.0",
  "private": true,
  "description": "Integration-testing framework for dsh-emacs-bridge: a seam harness that boots the real plugin against a live DeepSeek Harness host with a mock LLM. Dev-only; never staged into the Emacs package tar.",
  "type": "module",
  "license": "GPL-3.0",
  "engines": {
    "node": "^22.0.0"
  },
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^4.1.8"
  }
}
